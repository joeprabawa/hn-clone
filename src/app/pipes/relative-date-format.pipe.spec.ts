import { RelativeDateFormatPipe } from './relative-date-format.pipe';

describe('RelativeDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new RelativeDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
